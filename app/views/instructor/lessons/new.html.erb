<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1>Add A New Lesson</h1>
  <%= simple_form_for @lesson, url: instructor_section_lessons_path(current_section) do |t| %>
    <%= t.input :title %>
    <%= t.input :subtitle %>
    <%= t.input :video %>
    <br />
    <%= t.submit "Add The Lesson", class: 'btn btn-primary' %>
    <br />
    <br />
  <% end %>  
</div>
